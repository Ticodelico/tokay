  <header>
     <nav
        class="
          flex flex-wrap
          items-center
          justify-between
          w-full
          py-4
          md:py-0
          px-4
          text-lg text-gray-800
 bg-gradient-to-r from-indigo-500 via-indigo-700 to-purple-900 
        "
      >
       <div> 
        <%= link_to root_path do %>
   <%= image_tag "tokay.png", class: "logo" %>
<% end %>
        </div>

       
       <div class="hidden w-full md:flex md:items-center md:w-auto" id="menu">
          <ul
            class="
              pt-4
              text-base text-gray-700
              md:flex
              md:justify-between 
              md:pt-0"
          >
            <li>
            <%= link_to t('.admin'), admin_restaurants_path, class: "md:p-4 py-2 block hover:text-white-400 text-white" if Current.user&.admin?%>
            </li>
            <li>
            <%= link_to t('.login'), new_session_path, class: "md:p-4 py-2 block hover:text-grey-400 text-white" unless Current.user %>
            </li>
            <li>
            <%= link_to t('.register'), new_user_path, class: "md:p-4 py-2 block hover:text-grey-white text-white" unless Current.user %>
            </li>
            <li>
              <%= link_to t('.logout'), session_path(Current.user.id), data: {
              turbo_method: :delete,
              turbo_confirm: t('common.confirm')
            }, class: "md:p-4 py-2 block hover:text-sky-800 text-sky-200" if Current.user %>
            </li>

          </ul>
        </div>
    </nav>
  </header>
  
